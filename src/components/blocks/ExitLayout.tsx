export default function ExitLayout({ title, backgroundImage, link }: any) {

    console.log(title, backgroundImage, link);

	return (
		<section
			className="relative flex min-h-screen h-screen w-full items-center bg-cover bg-bottom bg-no-repeat"
		>
            Exit Layout
		</section>
	)
}
