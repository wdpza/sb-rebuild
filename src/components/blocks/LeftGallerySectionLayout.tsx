export default function LeftGallerySectionLayout({ description, gallery, title }: any) {

    console.log(description);
    console.log(gallery);
    console.log(title);

    return (
        <div>

        </div>
    );
}
