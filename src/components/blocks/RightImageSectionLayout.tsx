export default function PortfolioTabsLayout({ description, image, title }: any) {

    console.log(description);
    console.log(image);
    console.log(title);

    return (
        <div>

        </div>
    );
}
