export default function TeamsSectionLayout({ description, subTitle, title }: any) {

    console.log(description);
    console.log(subTitle);
    console.log(title);
    
    return (
        <div>

        </div>
    );
}
